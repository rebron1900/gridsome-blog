"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasDirective = exports.getDirective = void 0;
const getDirective = (node, name, argument) => node.startTag.attributes.find((a) => a.directive &&
    a.key.name.name === name &&
    (argument === undefined ||
        (a.key.argument &&
            a.key.argument.type === "VIdentifier" &&
            a.key.argument.name) === argument));
exports.getDirective = getDirective;
const hasDirective = (node, name, argument) => Boolean(exports.getDirective(node, name, argument));
exports.hasDirective = hasDirective;
